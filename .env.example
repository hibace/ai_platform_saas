# Copy to .env and fill in: cp .env.example .env
# Docker Compose will substitute these variables into services.

# --- LLM (required for agents) ---
# OpenAI: https://api.openai.com + key from https://platform.openai.com/api-keys
# Ollama (local): http://host.docker.internal:11434, no key needed
LLM_BASE_URL=https://api.openai.com
LLM_API_KEY=
LLM_DEFAULT_MODEL=gpt-4o-mini

# --- Optional: override DB (default uses postgres/mongodb/redis containers) ---
# POSTGRES_PASSWORD=postgres
# ConnectionStrings are set in docker-compose from service names; for your own DBs edit docker-compose or env in api.
